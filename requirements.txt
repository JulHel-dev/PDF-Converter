# Universal File Converter - Dependencies
# Core dependencies for all converters

# PDF Processing
PyMuPDF>=1.23.0  # fitz - PDF reading and rendering

# Document Processing
python-docx>=1.1.0  # Word document processing
openpyxl>=3.1.0  # Excel/spreadsheet processing

# Image Processing
Pillow>=10.0.0  # Image manipulation and conversion

# Data Format Processing
PyYAML>=6.0  # YAML support
markdown>=3.5.0  # Markdown to HTML conversion

# File Type Detection
python-magic-bin>=0.4.14; platform_system == "Windows"  # Windows binary for libmagic
python-magic>=0.4.27; platform_system != "Windows"  # Unix/Mac libmagic wrapper

# GUI
streamlit>=1.28.0  # Web-based GUI

# CLI Enhancements
colorama>=0.4.6  # Colored terminal output (optional)

# Packaging (for building .exe)
pyinstaller>=6.0.0  # Create standalone executables

# Optional: Advanced conversions
# reportlab>=4.0.0  # Advanced PDF generation
# pdfkit>=1.0.0  # HTML to PDF (requires wkhtmltopdf)
# pandoc>=2.3  # Universal document converter (wrapper)
